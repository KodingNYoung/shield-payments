import { Geist, Inter } from "next/font/google"

export const geist = Geist({
  variable: "--font-geist",
  subsets: ["latin"],
})

export const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
})
